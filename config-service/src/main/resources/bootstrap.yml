server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/belentoledo96/Microservice-Spring-Boot-Spring-Cloud.git
          searchPaths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}

  security:
    user:
      name: root
      password: s3cr3t